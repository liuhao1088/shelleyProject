<template name="detail">
  <view class="container">
    <view class="header">
      <image mode="widthFix" src="{{url}}">
      </image>
    </view>

    <view class="header" wx:if="{{urlTwo}}">
      <image mode="widthFix" src="{{urlTwo}}">
      </image>
    </view>

    <view class="footer">
      <image mode="widthFix" src="{{imgUrl}}">
      </image>
    </view>

  </view>
</template>